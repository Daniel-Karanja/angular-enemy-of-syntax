import { RenderDirectiveDirective } from './render-directive.directive';

describe('RenderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
